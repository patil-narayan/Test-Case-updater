# Demo Spring Boot CRUD

Simple Spring Boot application with CRUD endpoints for `Item`, Swagger UI, and tests.

Run tests:

```bash
mvn test
```

Start app:

```bash
mvn spring-boot:run
```

Swagger UI will be available at `http://localhost:8080/swagger-ui/index.html` when running.
